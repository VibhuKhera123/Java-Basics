class first{
     public static void main(String[] args) {
        bird parrot = new bird("parrot","delhi");
        // bird crow = new bird();
        // bird sparrow = new bird();
        parrot.lifetime = 20;
        parrot.speed = 100;
        
        parrot.display();

        // crow.lifetime = 50;
        // crow.speed = 120;
       
        // crow.display("crow");

        // sparrow.lifetime = 20;
        // sparrow.speed = 100;
        
        // sparrow.display("sparrow");
        // sparrow.sum(20, 10);
     }
}

